# Smithery configuration for alloyfyi
# https://smithery.ai/docs/build/project-config
startCommand:
  type: stdio
  configSchema:
    type: object
    required: []
    properties: {}
  commandFunction: |-
    config => ({ command: 'uvx', args: ['--from', 'alloyfyi[mcp]', 'python', '-m', 'alloyfyi.mcp_server'] })
